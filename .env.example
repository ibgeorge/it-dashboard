# Environment Variables Template for IT Dashboard
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (which is gitignored)
# 2. Replace placeholder values with your actual configuration
# 3. NEVER commit the .env file to version control
# 4. Keep this .env.example file updated as new variables are added

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Database Server (hostname or IP)
DB_SERVER=localhost

# Database Name
DB_NAME=ITDashboard

# Authentication Method (true = Windows Integrated, false = Basic Auth)
DB_USE_INTEGRATED_SECURITY=true

# Basic Authentication (only if DB_USE_INTEGRATED_SECURITY=false)
# Uncomment and set if using SQL Server authentication
# DB_USERNAME=your_database_username
# DB_PASSWORD=your_secure_database_password

# Database Connection Pool Settings
DB_MAX_CONNECTIONS=50
DB_CONNECTION_TIMEOUT=30000
DB_IDLE_TIMEOUT=600000
DB_MAX_LIFETIME=1800000

# ============================================
# APPLICATION SETTINGS
# ============================================

# Server Configuration
SERVER_PORT=8080
SERVER_CONTEXT_PATH=/

# Application Environment (development, staging, production)
APP_ENV=development

# Logging Level (ERROR, WARN, INFO, DEBUG, TRACE)
LOG_LEVEL=INFO

# ============================================
# SECURITY CONFIGURATION
# ============================================

# JWT Secret Key (Generate a strong random string - minimum 256 bits)
# Example generation: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_AT_LEAST_256_BITS

# JWT Token Expiration (in seconds)
JWT_EXPIRATION=3600

# Session Timeout (in seconds)
SESSION_TIMEOUT=3600

# Allowed Origins for CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# ============================================
# FEATURE FLAGS
# ============================================

# Enable/Disable Real-Time Updates
ENABLE_REAL_TIME=true

# Enable/Disable Data Export
ENABLE_EXPORT=true

# Enable/Disable Scheduled Reports
ENABLE_SCHEDULED_REPORTS=true

# Enable/Disable Email Notifications
ENABLE_EMAIL_NOTIFICATIONS=false

# ============================================
# EXTERNAL SERVICES
# ============================================

# Email Service (SMTP Configuration)
# Only needed if ENABLE_EMAIL_NOTIFICATIONS=true
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_email@example.com
SMTP_PASSWORD=your_email_password
SMTP_FROM_ADDRESS=noreply@example.com
SMTP_FROM_NAME=IT Dashboard

# Redis Cache (if using distributed cache)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# OAuth 2.0 Configuration (if using OAuth)
# OAUTH_CLIENT_ID=your_client_id
# OAUTH_CLIENT_SECRET=your_client_secret
# OAUTH_REDIRECT_URI=http://localhost:8080/oauth/callback
# OAUTH_AUTHORIZATION_URL=https://provider.com/oauth/authorize
# OAUTH_TOKEN_URL=https://provider.com/oauth/token

# ============================================
# API INTEGRATIONS
# ============================================

# External API Keys (add as needed for your integrations)
# EXTERNAL_API_KEY_1=your_api_key_here
# EXTERNAL_API_KEY_2=your_api_key_here

# ============================================
# MONITORING & OBSERVABILITY
# ============================================

# Application Insights / New Relic / DataDog
# APM_KEY=your_apm_key
# APM_SERVICE_NAME=it-dashboard

# ============================================
# FILE STORAGE
# ============================================

# Local file storage path for uploads/exports
FILE_STORAGE_PATH=./storage

# Maximum file upload size (in bytes)
MAX_FILE_UPLOAD_SIZE=10485760

# ============================================
# PERFORMANCE TUNING
# ============================================

# Thread Pool Size
THREAD_POOL_SIZE=10

# Query Timeout (in seconds)
QUERY_TIMEOUT=30

# Cache TTL (in seconds)
CACHE_TTL=300

# ============================================
# NOTES
# ============================================
# 
# Security Best Practices:
# - Use strong, unique passwords for all credentials
# - Rotate secrets regularly
# - Use a secrets management service in production (Azure Key Vault, AWS Secrets Manager, HashiCorp Vault)
# - Never commit actual values to version control
# - Use different values for each environment (dev, staging, prod)
# - Grant minimal required permissions to service accounts
# 
# For Windows Integrated Security:
# - Ensure the application runs under a Windows account with database access
# - Add the Windows account to the SQL Server with appropriate permissions
# - No username/password needed in connection string
# 
# For Production:
# - Set LOG_LEVEL=INFO or WARN
# - Enable HTTPS/TLS
# - Use strong JWT_SECRET (64+ characters)
# - Configure proper CORS origins
# - Enable all security features
